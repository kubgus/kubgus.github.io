---
import NavbarLayout from "./navbar.astro";

const { title, meta } = Astro.props || {
    title: "Jakub Gustafik",
    meta: [],
}
---

<NavbarLayout title={title} meta={meta} fixed={true}>
    <slot />
</NavbarLayout>
